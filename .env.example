# Copy to .env (git-ignored) and fill in. Used by ./bin/*.sh only.
DOCKHAND_API_TOKEN=
DOCKHAND_URL=http://jiban.lan:3000
DOCKHAND_ENV_ID=1

VPS_USER=bruno
VPS_HOST=jiban.lan

# Host directory that stores the SQLite database (megasena.db + -wal/-shm).
# Pushed to Dockhand as a secret env override so the real path stays out of git.
LOTERIAS_DATA_DIR=
